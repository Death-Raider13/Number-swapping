# Fill in your details here before running the bot.

# Your WhatsApp number in international format, no + or spaces.
# Messages from this number are treated as admin commands.
ADMIN_NUMBER = "2348000000000"

# Path where Chrome will store its session (so you only scan QR once).
CHROME_PROFILE_PATH = "./chrome_profile"

# Path to config.json (bot content — menus, flows, replies, schedules).
CONFIG_PATH = "./config.json"

# --- Google Sheets integration (optional) ---
# Path to your Google service account JSON key file.
GOOGLE_CREDENTIALS_FILE = "./google_credentials.json"
# The name (or ID) of the Google Spreadsheet to log flow completions into.
GOOGLE_SHEET_NAME = "WhatsApp Bot Orders"

# --- Email / SMTP integration (optional) ---
SMTP_HOST = "smtp.gmail.com"
SMTP_PORT = 587
SMTP_USER = "your_email@gmail.com"
SMTP_PASSWORD = "your_app_password"
ADMIN_EMAIL = "your_email@gmail.com"
